//스택
#include<iostream>
#include<stack>
using namespace std;

int main() {
	int n;
	stack<int> s;
	cin >> n;
	for (int i = 0; i < n; i++) {
		int a;
		cin >> a;
		s.push(a);
	}
	for (int i = 0; i < n; i++) {
		cout << s.top() << endl;
		s.pop();
	}
}
