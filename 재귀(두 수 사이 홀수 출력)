#include<iostream>
using namespace std;


int factorial(int a,int b) {
	if (a % 2 == 1)
		cout << a << " ";
	a++;
	if (a > b)
		return 0;
	return factorial(a, b);

}
int main() {
	int a, b;
	cin >> a; cin >> b;
	factorial(a, b);
}
