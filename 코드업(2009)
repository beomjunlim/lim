#include <iostream>
using namespace std;

int main()
{
  ios::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);
  int n, k;
  int cnt = 0;
  cin >> k;
  cin >> n;
  while (n <= k)
  {
    k -= n;
    cnt++;
    k++;
  }
  cout << cnt << '\n';
}
