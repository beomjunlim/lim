//배열을 입력 받고 첫째줄에 짝수번째 배열 출력 두번째줄에 홀수번째 배열 출력

#include <iostream>
using namespace std;

int main()
{
  int n;
  cin >> n;
  int arr[n];
  for (int i = 0; i < n; i++)
  {
    cin >> arr[i];
  }
  for (int i = 1; i < n; i += 2)
  {
    cout << arr[i] << " ";
  }
  cout << endl;
  for (int i = 0; i < n; i += 2)
  {
    cout << arr[i] << " ";
  }
}
