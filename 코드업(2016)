#include<iostream>
using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	int n,k;
	char arr[201]={0};
	cin >> n;
	cin >> arr;
	k = n % 3;
	for (int i = 0; i < n; i++) {
		if ((i % 3 == k) && i > 0)
			cout << ",";
		cout << arr[i];
	}
}
